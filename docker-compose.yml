version: "3.9"

services:
  postgres:
    image: postgres:15
    container_name: seo_ai_postgres
    environment:
      POSTGRES_USER: seo
      POSTGRES_PASSWORD: seo
      POSTGRES_DB: seo_ai
    ports:
      - "5432:5432"
